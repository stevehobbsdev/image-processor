
namespace Simplicode.Imaging
{
	/// <summary>
	/// Represents a method which handles evetns concerning the processing of an image.
	/// </summary>
	/// <param name="sender">The image processor which is processing the image.</param>
	/// <param name="e">The event arguments</param>
	public delegate void ImageEventHandler(object sender, ImageEventArgs e);
}
